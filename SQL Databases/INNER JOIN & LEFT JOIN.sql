/*	SELECT
		A.artistid,
		A.name,
		B.title
	FROM artists AS A
	INNER JOIN albums AS B
	ON A.artistid = B.artistid; */
/*	SELECT
		A.artistid,
		A.name,
		B.title
	FROM artists AS A
	LEFT JOIN albums AS B
	ON A.artistid = B.artistid; */